<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title>Directions service</title>
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
      #floating-panel {
        position: absolute;
        top: 10px;
        left: 25%;
        z-index: 5;
        background-color: #fff;
        padding: 5px;
        border: 1px solid #999;
        text-align: center;
        font-family: 'Roboto','sans-serif';
        line-height: 30px;
        padding-left: 10px;
      }
    </style>
  </head>
  <body>
    <h3>
      <%= @uri %>
    </h3>
    <iframe
      width="1000"
      height="600"
      frameborder="0" style="border:0"
      src="<%= @uri %>" allowfullscreen>
      </iframe>
    /*iframe
      width="600"
      height="450"
      frameborder="0" style="border:0"
      src="https://www.google.com/maps/embed/v1/directions?key=AIzaSyDGCAVFl_cfm8s6vAj5fDhKy9AsQ9HHFy0
        &origin=Av.+Kennedy,+2000,+Curitiba+Brazil
  &destination=Pato+Branco+Brazil&waypoints=Rua+Izabel+a+Redento%2C+1570+S%C3%A3o+Jos%C3%A9+dos+Pinhais+PR" allowfullscreen>
       /iframe> */
  </body>
</html>
